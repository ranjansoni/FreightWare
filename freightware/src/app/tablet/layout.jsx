export const metadata = {
  title: 'Floor Assistant — FreightWare',
};

export default function TabletLayout({ children }) {
  return (
    <div className="min-h-screen bg-gray-50 text-gray-900 font-sans">
      {children}
    </div>
  );
}
